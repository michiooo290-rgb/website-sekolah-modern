# Paket yang dipasang lewat `npm install <nama>` dikunci ke versi persis,
# bukan disimpan sebagai rentang "^". Bersama package-lock.json ini menjaga
# hasil build tetap sama dari waktu ke waktu, dan menutup celah rantai pasok
# ketika ada rilis baru yang bermasalah atau disusupi.
save-exact=true
