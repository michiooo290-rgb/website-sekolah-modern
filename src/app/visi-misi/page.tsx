import { PageHero } from "@/components/page-hero";
import { getVision } from "@/lib/data";

export const metadata={title:"Visi & Misi"};
export default async function VisionPage(){const items=await getVision();const vision=items.find(x=>x.tipe==="visi");const missions=items.filter(x=>x.tipe==="misi");const values=items.filter(x=>x.tipe==="nilai");return <><PageHero eyebrow="Arah Pendidikan" title="Visi yang besar, langkah yang nyata." description="Arah bersama yang menuntun pendidikan, pembinaan karakter, dan pelayanan sekolah."/><section className="section"><div className="container"><article className="card" style={{background:"var(--pine)",color:"white",padding:"clamp(2rem,6vw,5rem)",textAlign:"center"}}><span className="eyebrow" style={{color:"var(--brass-light)"}}>Visi</span><h2 style={{maxWidth:850,margin:"1rem auto 0"}}>“{vision?.isi}”</h2></article></div></section><section className="section alt"><div className="container grid two"><div><span className="eyebrow">Misi</span><h2>Langkah yang kami kerjakan setiap hari.</h2></div><div className="number-list">{missions.map(x=><article className="number-item" key={x.id}><div><h3>{x.judul??"Komitmen Pendidikan"}</h3><p>{x.isi}</p></div></article>)}</div></div></section><section className="section"><div className="container"><div className="section-head"><div><span className="eyebrow">Nilai Utama</span><h2>Karakter yang kami tumbuhkan.</h2></div></div><div className="grid four">{values.map(x=><article className="card" key={x.id}><h3>{x.judul}</h3><p>{x.isi}</p></article>)}</div></div></section></>}
